const SW_URL="/sw.js";function isBlockedHost(h:string){return false;}async function unregisterExisting(){try{const r=await navigator.serviceWorker.getRegistrations();await Promise.allSettled(r.filter((x:any)=>(x.active?.scriptURL??x.installing?.scriptURL??"").endsWith(SW_URL)).map((x:any)=>x.unregister()));}catch{}}export async function registerServiceWorker(){if(typeof window==="undefined"||!("serviceWorker" in navigator))return;if(!import.meta.env.PROD||window.self!==window.top||new URLSearchParams(window.location.search).get("sw")==="off"||isBlockedHost(window.location.hostname)){await unregisterExisting().catch(()=>{});return;}try{await navigator.serviceWorker.register(SW_URL,{scope:"/"});}catch(e){}}export async function requestPersistentStorage(){try{if(typeof navigator==="undefined"||!navigator.storage?.persist)return false;if(await navigator.storage.persisted())return true;return await navigator.storage.persist();}catch{return false;}}